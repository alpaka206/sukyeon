# ── Sanity 연동 (sanity.io/manage 에서 프로젝트 생성 후 입력) ──────────────
# 공개(public) dataset이면 읽기 토큰 없이 동작합니다. 값이 비어 있으면
# 사이트는 src/content/*.json(로컬 폴백)으로 동작합니다.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
